# 玩家指南 — DualRoleAdventure（本地多角色操控 / LocalMultiControl）

> English guide: [PLAYER_GUIDE.md](PLAYER_GUIDE.md)

在《杀戮尖塔 2》的多人模式中，在一台机器上、不联网，独自操控 2–12 名角色。

## 安装与启用

1. 在 Steam 创意工坊订阅（或把 `DualRoleAdventure.dll` + `DualRoleAdventure.json`
   放到 `<游戏目录>\mods\DualRoleAdventure\`）。
2. 启动游戏 → `设置 → Mods` → 启用 **多角色冒险 (DualRoleAdventure)**。如提示请重启游戏。

## 开局

1. 主菜单 → **多人游戏 → 主机（Host）**。
2. 选择 **Local Multi-Control（单人多角色）** 卡片（在 标准/每日/自定义 旁边）。
3. 角色选择界面：
   - **`+` / `-`** — 增加或减少本地角色数量（2–12，允许重复角色）；
   - **`Tab` / `Shift+Tab`** — 切换当前编辑的角色；
   - 给每个位置选好角色并准备，然后正常开局。
4. **自定义模式**同样可用：从多人菜单进入 `自定义模式` 也会走本地多控流程，
   可以配合种子与自定义规则开多角色局。

## 瓦库（AI 自动打牌）

任何角色都可以交给内置自动玩家"瓦库"托管：

- 角色选择界面：逐个切换勾选，或一键全选；
- 手柄：`Y` 切换高亮角色的瓦库勾选，`LT + Y` 一键全体；
- 被托管的角色自动完成回合；当没有瓦库角色可行动时，控制权回到你手上。

### 瓦库形态（v1.36 新增，默认关）

可选的进阶托管模式。开启后，被勾选的角色改发独立遗物
【瓦库形态】（描述："让瓦库玩算你赢了。"），替代"永久低语耳环"；
瓦库全程**后台**打牌——游戏不再切到它的视角，并且每回合打光所有手牌。

配置方式二选一：

- 配置文件 `%APPDATA%\SlayTheSpire2\vakuu_autopilot.json`（每次开局重新加载）；
- 游戏内 **设置 → 常规 → 「瓦库托管」子菜单**（原生观感，改动即时生效并保存）。

| 配置项 | 默认 | 说明 |
|---|---|---|
| `useVakuuForm` | `false` | 总开关。false = 与旧版"永久低语耳环"行为完全一致 |
| `playAllCards` | `true` | 打光所有手牌（60 张护栏防死循环） |
| `backgroundMode` | `true` | 后台托管——不再为瓦库切换前台；弹层卡住超时（战斗 12 秒/事件 8 秒）安全网会切回前台提示 |
| `suppressVanillaEarring` | `true` | 持有【瓦库形态】时压制原版低语耳环的自动出牌钩子（+1 能量保留） |
| `autoClaimCards` | `true` | 战后瓦库的卡牌奖励自动领最左；金币与遗物同规则 |
| `autoClaimGoldRelics` | `true` | 金币与遗物奖励自动领取 |
| `autoClaimPotions` | `true` | 药水奖励自动领取。有空位直接领；满栏时先喝栏内鲜血药水腾位；否则奖励稀有度高于栏内最低才丢弃栏内最低者换领，等价或更低放弃 |
| `autoChooseEvents` | `true` | 非共享事件自动选择（策略见下）。致死选项会被剔除；触发战斗/小游戏/拿不准的情况停住等你处理 |
| `eventChoiceMode` | `first` | 事件选项策略：first=第一个 / last=最后一个 / random=随机 |
| `cardPickMode` | `last` | 战斗内效果选牌（合成、选 N 张等）取哪张。卡牌奖励始终领最左 |
| `autoRestChoice` | `true` | 火堆自动选择：血量 <50% 睡觉；有遗物选项（举重/挖掘等）在睡觉以外随机；否则锻造升级"打击/防御以外"的最后一张可升级牌（不足用打击/防御补齐，全升完则睡觉）；未满血且没得锻时愈合队友；全员满血时打击/防御也照升；帐篷多选全拿 |
| `neowAutoChoose` | `false` | 是否连涅奥开局奖励也自动选 |
| `autoUsePotions` | `false` | 战斗中按逐药规则表自动用药：血液/再生低血(<50%)自用；果汁到手立刻喝；混沌药水在栏内只剩它且有空位时喝；力量等增益类与火焰等攻击类、攻击/技能/能力/无色药水在精英/Boss 战首回合使用；格挡/免伤类在回合结束前按敌方意图伤害兜底；故障机器人/储君/亡灵契约师/铁甲战士专属药水自动给对应队友；复制/超巨化优先给真人；灰水/赌徒特酿等定向消耗坏牌；污浊药水绝不在战斗中使用、遇商人自动投掷换钱；mod 药水普通战斗随机回合消耗 |

### 瓦库会自动做哪些事（v1.37）

打开上述开关后，后台瓦库还会：领取自己的战后/事件奖励、解决非共享事件、
自动选火堆选项、按规则表用药水、把污浊药水投掷给商人换钱。
商店购物、共享事件、水晶球占卜仍由你手动完成。

## 对局中

- **切换角色：** `Tab`（下一个）/ `Shift+Tab`（上一个）。旧按键 `]` `R` `/`（下一个）、
  `[` `T`（上一个）仍然可用。
- 每个角色拥有独立的卡组、手牌、能量、金币、药水、遗物与选择；界面（手牌/能量/药水栏/
  状态条）跟随你当前控制的角色。
- **战斗：** 依次操作每个角色的回合，随时切换；每个角色分别结束回合。
- **奖励：** 战利品按角色独立生成，汇总为一个列表展示，每项前缀归属角色
  （如 `[Player 2]`）；由对应角色领取（开启自动领取后瓦库的部分会自动结算）。
- **事件：** 默认每个角色独立解决事件，mod 会引导你逐个完成；需要全员投票的共享事件
  会自动补票。非共享事件可交给瓦库自动选择。
- **水晶球：** 每个角色在自己的回合付费占卜；揭示出的奖励（以及分期付款诅咒）只归揭示者。
  占卜进行期间锁定切人——先完成占卜，mod 会自动切换。
- **火堆：** 每个角色依次选择（睡觉、锻造等）；可交给瓦库自动选择。
- **商店：** 购买与删牌按当前控制角色结算。假商人事件（商人？？？）同理——每个角色
  用自己的金币浏览和购买自己的货架，任意角色都可以向商人投掷污浊药水开启战斗。
- **地图：** 点选下一个节点后自动完成"全员投票"步骤。
- **保存与继续：** 正常退出即可；`多人游戏 → 载入` 恢复对局并自动让所有本地角色就绪。

## 幽灵手牌浮层（可选）

在当前手牌后方显示其他角色的手牌，方便统筹全队规划：

- **`F8`** — 开/关（默认关；跨会话记忆）
- **`Ctrl+方向键`** — 移动位置（按住平滑移动）；**`Ctrl+Shift+方向键`** — 4 像素微调
- 卡牌半透明、鼠标穿透，随角色抽牌/出牌实时更新。
- 设置保存在 `%APPDATA%\SlayTheSpire2\dual_role_adventure_settings.json`；
  修改其中的 `ghostHandsScale`（默认 `0.5`）可调整卡牌大小。

## 快捷键速查

| 按键 | 场景 | 功能 |
|---|---|---|
| `Tab` / `Shift+Tab` | 任意 | 切换控制角色（下一个/上一个） |
| `[` `T` / `]` `R` `/` | 任意 | 旧版切换别名（上一个/下一个） |
| `+` / `-` | 大厅 | 增减本地角色数量（2–12） |
| `F8` | 战斗 | 开关幽灵手牌浮层 |
| `Ctrl+方向键`（+Shift） | 战斗且浮层开启 | 移动幽灵手牌（Shift 为微调） |
| `Y` / `LT+Y` | 选人界面（手柄） | 切换单个/全部角色的瓦库勾选 |
| `LT + 十字键` | 选人界面（手柄） | 角色数量与编辑位控制 |

## 问题反馈

- 日志文件：`%APPDATA%\SlayTheSpire2\logs\godot.log`——本 mod 的日志带
  `[LocalMultiControl]` 前缀。
- 遇到问题时请记录**幕数、房间/界面与准确步骤**，附上日志到
  [GitHub Issues](https://github.com/nanthepsmith-droid/STS2_DualRoleAdventure/issues) 反馈。
- 已知待查问题见 [TODO.md](TODO.md)。
